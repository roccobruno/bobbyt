model:


//Account
//FBAccount
//Journey(recurring? + jmt + duration? + startsAt)
//JourneyMeansOfTransportation  or JMT (tube lines and trains)
//Alert
//EmailAlert

//JobForJack(runFrom + runTo + alertSent + recurring)

// ++ TFL model
// ++ TRAIN model

scheduledJobs to create:

- Job Processing(recurring) = find recurring jobs + create alerts + actor
- Job Processing(one-time)
- Alert Processing
- TFL tube checker -- OK
- Train lines checker
- Job Reset - find jobs to reset + reset jobs + actor


